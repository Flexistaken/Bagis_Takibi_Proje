<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.example.bagis_takibi_proje.BagisFormController"
      stylesheets="@style.css"
      spacing="20"
      alignment="CENTER"
      prefWidth="400"
      style="-fx-background-color: white; -fx-background-radius: 15;">

    <padding>
        <Insets top="30" right="40" bottom="30" left="40"/>
    </padding>

    <VBox spacing="5" alignment="CENTER">
        <Label text="💰 Bağış İşlemi" styleClass="panel-title" style="-fx-font-size: 20;"/>
        <Label text="Lütfen bağış detaylarını doldurunuz." style="-fx-text-fill: #7f8c8d; -fx-font-size: 12;"/>
    </VBox>

    <Separator opacity="0.4"/>

    <VBox spacing="15">
        <VBox spacing="5">
            <Label text="Seçili Kurum ID" style="-fx-font-weight: bold; -fx-text-fill: #34495e; -fx-font-size: 11;"/>
            <TextField fx:id="kurumIdField" editable="false" styleClass="modern-input"
                       style="-fx-background-color: #f1f2f6; -fx-text-fill: #7f8c8d;"/>
        </VBox>

        <VBox spacing="5">
            <Label text="Bağış Miktarı (TL)" style="-fx-font-weight: bold; -fx-text-fill: #34495e; -fx-font-size: 11;"/>
            <TextField fx:id="miktarField" promptText="0.00" styleClass="modern-input"/>
        </VBox>

        <VBox spacing="5">
            <Label text="Açıklama / Not" style="-fx-font-weight: bold; -fx-text-fill: #34495e; -fx-font-size: 11;"/>
            <TextField fx:id="aciklamaField" promptText="Kısa bir not ekleyin..." styleClass="modern-input"/>
        </VBox>
    </VBox>

    <HBox spacing="15" alignment="CENTER_RIGHT">
        <padding>
            <Insets top="10"/>
        </padding>
        <Button text="Vazgeç" onAction="#iptal"
                style="-fx-background-color: transparent; -fx-text-fill: #95a5a6; -fx-cursor: hand; -fx-font-weight: bold;"/>

        <Button text="Bağışı Tamamla" onAction="#bagisYap"
                styleClass="btn-primary" prefWidth="160"/>
    </HBox>

</VBox>