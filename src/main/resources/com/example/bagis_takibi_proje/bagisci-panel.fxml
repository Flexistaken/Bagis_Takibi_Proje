<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.bagis_takibi_proje.BagisciController"
            stylesheets="@style.css"
            styleClass="panel-gradient"
            prefWidth="950" prefHeight="600">

    <!-- SOL MENÜ -->
    <left>
        <VBox styleClass="sidebar">
            <Label text="BAĞIŞÇI PANELİ" styleClass="sidebar-title"/>

            <Separator opacity="0.3"/>

            <Region VBox.vgrow="ALWAYS"/>

            <Button text="🚪 Çıkış Yap"
                    styleClass="logout-button"
                    onAction="#cikisYap"
                    maxWidth="Infinity"/>
        </VBox>
    </left>

    <!-- ORTA ALAN -->
    <center>
        <ScrollPane fitToWidth="true" styleClass="transparent-scroll">
            <VBox spacing="30" styleClass="content-area">

                <!-- BAŞLIK -->
                <VBox spacing="6">
                    <Label fx:id="welcomeLabel"
                           text="Hoş Geldiniz 👋"
                           styleClass="panel-title"/>
                    <Label text="Bağış yaparak iyiliğe katkıda bulun."
                           style="-fx-text-fill: #ecf0f1; -fx-font-size: 14;"/>
                </VBox>

                <!-- MINI DASHBOARD -->
                <HBox spacing="20">
                    <VBox styleClass="dashboard-card">
                        <Label text="Toplam Bağışım" styleClass="dashboard-card-title"/>
                        <Label fx:id="toplamBagisimLabel" text="0 ₺"
                               styleClass="dashboard-card-value"/>
                    </VBox>

                    <VBox styleClass="dashboard-card">
                        <Label text="Bu Ay" styleClass="dashboard-card-title"/>
                        <Label fx:id="aylikBagisLabel" text="0 ₺"
                               styleClass="dashboard-card-value"/>
                    </VBox>

                    <VBox styleClass="dashboard-card">
                        <Label text="Son Bağış" styleClass="dashboard-card-title"/>
                        <Label fx:id="sonBagisLabel" text="-"
                               styleClass="dashboard-card-value"/>
                    </VBox>
                </HBox>

                <!-- BAĞIŞ YAP -->
                <VBox styleClass="card" spacing="18">
                    <Label text="🎁 Bağış Yap"
                           style="-fx-font-weight: bold; -fx-font-size: 18px;"/>

                    <ComboBox fx:id="kurumCombo"
                              promptText="Kurum seçiniz"
                              styleClass="admin-input"/>

                    <ComboBox fx:id="turCombo"
                              promptText="Bağış türü seçiniz"
                              styleClass="admin-input"/>

                    <TextField fx:id="miktarField"
                               promptText="Bağış miktarı (₺)"
                               styleClass="admin-input"/>

                    <TextField fx:id="aciklamaField"
                               promptText="Açıklama "
                               styleClass="admin-input"/>


                    <Button fx:id="bagisYapBtn"
                            text="Bağış Yap"
                            onAction="#bagisYap"
                            styleClass="btn-primary"
                            prefHeight="42"/>
                </VBox>

                <!-- GEÇMİŞ BAĞIŞLAR -->
                <VBox styleClass="card" spacing="12">
                    <Label text="📜 Geçmiş Bağışlarım"
                           style="-fx-font-weight: bold; -fx-font-size: 16px;"/>

                    <TableView fx:id="bagisTable" prefHeight="220">
                        <columns>
                            <TableColumn fx:id="bagisKurumColumn" text="Kurum"/>
                            <TableColumn fx:id="bagisTurColumn" text="Tür"/>
                            <TableColumn fx:id="bagisMiktarColumn" text="Miktar"/>
                            <TableColumn fx:id="bagisTarihColumn" text="Tarih"/>
                        </columns>
                        <columnResizePolicy>
                            <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
                        </columnResizePolicy>
                    </TableView>
                </VBox>

            </VBox>
        </ScrollPane>
    </center>

</BorderPane>
