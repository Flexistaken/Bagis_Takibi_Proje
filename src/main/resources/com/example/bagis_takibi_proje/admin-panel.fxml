<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.bagis_takibi_proje.AdminController"
            stylesheets="@style.css"
            styleClass="panel-gradient"
            prefWidth="1000"
            prefHeight="650">

    <!-- SOL MENÜ -->
    <left>
        <VBox styleClass="sidebar">
            <Label text="YÖNETİM PANELİ" styleClass="sidebar-title"/>

            <Separator opacity="0.3"/>

            <ToggleButton fx:id="btnKurumEkle"
                          text="➕ Kurum Ekle"
                          onAction="#kurumEkle"
                          styleClass="menu-toggle"
                          maxWidth="Infinity"/>

            <ToggleButton fx:id="btnKurumGuncelle"
                          text="🔄 Kurum Güncelle"
                          onAction="#kurumGuncelle"
                          styleClass="menu-toggle"
                          maxWidth="Infinity"/>

            <ToggleButton fx:id="btnKurumSil"
                          text="🗑️ Kurum Sil"
                          onAction="#kurumSil"
                          styleClass="menu-toggle"
                          maxWidth="Infinity"/>

            <Separator opacity="0.3"/>

            <ToggleButton fx:id="btnBagisSil"
                          text="❌ Bağış Sil"
                          onAction="#bagisSil"
                          styleClass="menu-toggle"
                          maxWidth="Infinity"/>

            <ToggleButton fx:id="btnRaporlar"
                          text="📊 Raporlar"
                          onAction="#raporlarAc"
                          styleClass="menu-toggle"
                          maxWidth="Infinity"/>

            <Region VBox.vgrow="ALWAYS"/>

            <Button text="🚪 Çıkış Yap"
                    onAction="#cikisYap"
                    styleClass="logout-button"
                    maxWidth="Infinity"/>
        </VBox>

    </left>

    <!-- SAĞ TARAF -->
    <center>
            <VBox spacing="20" styleClass="content-area">

                <HBox alignment="CENTER" spacing="20">

                    <!-- SOL -->
                    <Label fx:id="adminWelcomeLabel"
                           styleClass="admin-welcome"/>

                    <!-- ORTA -->
                    <Label text="Genel Bakış"
                           styleClass="panel-title"
                           HBox.hgrow="ALWAYS"
                           maxWidth="Infinity"
                           alignment="CENTER"/>

                    <!-- SAĞ (denge için boş) -->
                    <Region prefWidth="250"/>
                </HBox>


                <!-- KURUM TABLOSU -->
                <VBox styleClass="card" spacing="12">
                    <Label text="Kurum Listesi"
                           style="-fx-font-weight: bold; -fx-font-size: 16px;"/>
                    <TextField fx:id="kurumSearchField"
                               promptText="Kurum ara..."
                               styleClass="admin-input"/>

                    <!-- Tabloya nefes aldırmak için wrap -->
                    <StackPane styleClass="table-wrap">
                        <TableView fx:id="kurumTable" styleClass="kurum-table" prefHeight="220">
                            <columns>
                                <TableColumn fx:id="idColumn" text="ID" prefWidth="80"/>
                                <TableColumn fx:id="adColumn" text="Kurum Adı" prefWidth="420"/>
                            </columns>
                        </TableView>
                    </StackPane>
                </VBox>

                <!-- BAĞIŞ TABLOSU -->
                <VBox styleClass="card" spacing="12">
                    <Label text="Son Bağışlar"
                           style="-fx-font-weight: bold; -fx-font-size: 16px;"/>

                    <StackPane styleClass="table-wrap">
                        <TableView fx:id="bagisTable" styleClass="bagis-table" prefHeight="260">
                            <columns>
                                <TableColumn fx:id="kurumColumn" text="Kurum"/>
                                <TableColumn fx:id="bagisciColumn" text="Bağışçı"/>
                                <TableColumn fx:id="turColumn" text="Tür"/>
                                <TableColumn fx:id="miktarColumn" text="Miktar (₺)"/>
                                <TableColumn fx:id="tarihColumn" text="Tarih"/>
                            </columns>
                        </TableView>
                    </StackPane>

                </VBox>

                <!-- MINI DASHBOARDLAR (TABLOLARDAN AYRI - AŞAĞIDA) -->
                <HBox spacing="20" styleClass="dashboard-cards" alignment="CENTER_LEFT">
                    <Label text="💰" style="-fx-font-size: 28px;"/>
                    <VBox styleClass="dashboard-card">
                        <Label text="Toplam Bağış" styleClass="dashboard-card-title"/>
                        <Label fx:id="toplamBagisLabel" text="0 ₺"
                               styleClass="dashboard-card-value"/>
                    </VBox>

                    <VBox styleClass="dashboard-card">
                        <Label text="Bugün Yapılan Bağış" styleClass="dashboard-card-title"/>
                        <Label fx:id="bugunBagisLabel" text="0 ₺"
                               styleClass="dashboard-card-value"/>
                    </VBox>
                </HBox>

            </VBox>
    </center>

</BorderPane>
