<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns:fx="http://javafx.com/fxml" fx:controller="com.example.bagis_takibi_proje.AdminController"
            stylesheets="@style.css" prefWidth="1000" prefHeight="650">

<<<<<<< HEAD
=======
    <!-- ================= ÜST BAŞLIK ================= -->
    <top>
        <HBox alignment="CENTER"
              style="-fx-padding: 10;"
              spacing="10">

            <!-- SOL: Hoş geldin -->
            <Label fx:id="welcomeLabel"
                   styleClass="welcome-text"/>

            <!-- Ortalamak için sol boşluk -->
            <Region HBox.hgrow="ALWAYS"/>

            <!-- ORTA: Panel Başlığı -->
            <Label text="ADMİN PANELİ"
                   styleClass="panel-title"/>

            <!-- Sağ boşluk (denge için) -->
            <Region HBox.hgrow="ALWAYS"/>

        </HBox>
    </top>


    <!-- ================= SOL MENU ================= -->
>>>>>>> e26c6781f9cf2e037616465145cc3fbd01673424
    <left>
        <VBox styleClass="sidebar">
            <Label text="YÖNETİM PANELİ" styleClass="sidebar-title"/>
            <Button text="➕ Kurum Ekle" onAction="#kurumEkle" styleClass="menu-button" maxWidth="Infinity"/>
            <Button text="🔄 Kurum Güncelle" onAction="#kurumGuncelle" styleClass="menu-button" maxWidth="Infinity"/>
            <Button text="🗑️ Kurum Sil" onAction="#kurumSil" styleClass="menu-button" maxWidth="Infinity"/>
            <Separator opacity="0.3"/>
            <Button text="❌ Bağış Sil" onAction="#bagisSil" styleClass="menu-button" maxWidth="Infinity"/>
            <Button text="📊 Raporlar" onAction="#raporlarAc" styleClass="menu-button" maxWidth="Infinity"/>
            <Region VBox.vgrow="ALWAYS"/> <Button text="🚪 Çıkış Yap" onAction="#cikisYap" styleClass="logout-button" maxWidth="Infinity"/>
        </VBox>
    </left>

    <center>
        <ScrollPane fitToWidth="true" style="-fx-background-color: transparent;">
            <VBox spacing="20" styleClass="content-area">
                <Label text="Genel Bakış" styleClass="panel-title"/>

                <VBox styleClass="card" spacing="10">
                    <Label text="Kurum Listesi" style="-fx-font-weight: bold; -fx-font-size: 16px;"/>
                    <TableView fx:id="kurumTable" prefHeight="250">
                        <columns>
                            <TableColumn fx:id="idColumn" text="ID" prefWidth="50"/>
                            <TableColumn fx:id="adColumn" text="Kurum Adı" prefWidth="400"/>
                        </columns>
                    </TableView>
                </VBox>

                <VBox styleClass="card" spacing="10">
                    <Label text="Son Bağışlar" style="-fx-font-weight: bold; -fx-font-size: 16px;"/>
                    <TableView fx:id="bagisTable" prefHeight="250">
                        <columns>
                            <TableColumn fx:id="bagisIdColumn" text="ID" prefWidth="50"/>
                            <TableColumn fx:id="bagisciIdColumn" text="Bağışçı" prefWidth="150"/>
                            <TableColumn fx:id="bagisKurumIdColumn" text="Kurum" prefWidth="150"/>
                            <TableColumn fx:id="bagisMiktarColumn" text="Miktar (TL)" prefWidth="100"/>
                        </columns>
                    </TableView>
                </VBox>
            </VBox>
        </ScrollPane>
    </center>
</BorderPane>